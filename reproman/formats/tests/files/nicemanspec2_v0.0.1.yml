# Minimalistic example for the reproman spec which lists only desired components
# So it is geared toward the user to provide a simple spec.
# It demonstrates also ability to list items simply by the name with possibly
# some additional semantic for version
version: "0.0.1"

# how to instruct to "enable neurodebian"?
distributions:
 debian:
   packages:
   - libc6-dev
   - connectome-workbench
 conda:
  packages:
   - nipype>=0.12.11

files:
 - "/opt/blah"


